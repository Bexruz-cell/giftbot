[app]
name = "giftbot-riyoshop"
primary_region = "ams"

[build]
dockerfile = "Dockerfile"

[env]

[[services]]
internal_port = 8080
protocol = "tcp"

[[vm]]
memory = "256mb"
cpu_kind = "shared"
cpus = 1
